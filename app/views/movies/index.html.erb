<p style="color: green"><%= notice %></p>

<div class="container">

  <h1>Peliculas</h1>
  <%= link_to "Nueva Pelicula", new_movie_path, class: "btn btn-primary" %>
  
  <div id="movies">
    <div class="row mt-3">
      <% @movies.each do |movie| %>
        <%= render movie %>
      <% end %>
    </div>
  </div>
</div>
