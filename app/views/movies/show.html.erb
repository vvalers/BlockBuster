<p style="color: green"><%= notice %></p>

<%= render @movie %>

<div class="container mt-3">
  <%= link_to "Editar", edit_movie_path(@movie), class: "btn btn-warning me-2" %>
  <%= link_to "Volver a películas", movies_path, class: "btn btn-secondary me-2" %>
  <%= button_to "Eliminar", @movie, method: :delete, class: "btn btn-danger mt-2" %>
</div>
